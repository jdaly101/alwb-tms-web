var templates = require('duality/templates'),
	fields = require('couchtypes/fields'),
	Form = require('couchtypes/forms').Form,
	session = require('session');

exports.login = function (doc, req) {
	/*
	var myForm = new Form ({username : fields.string(), password : fields.string()});
	myForm.validate(req);

	if (myForm.values.user && myForm.values.pass)
		session.login(myForm.values.username, myForm.values.password, {});

	return [doc, 'placeholder'];
	*/
};

